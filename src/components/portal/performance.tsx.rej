diff a/src/components/portal/performance.tsx b/src/components/portal/performance.tsx	(rejected hunks)
@@ -30,7 +30,7 @@ export function MyPerformanceChart() {
       if (error) throw error;
       return recs || [];
     }).then(recs => {
-      const byDate = new Map((recs || []).map((r: any) => [r.attendance_date, r]));
+      const byDate = new Map((recs || []).map(r => [r.attendance_date, r]));
       const days: { day: string; hours: number }[] = [];
       for (let i = 13; i >= 0; i--) {
         const d = new Date(); d.setDate(d.getDate() - i);
@@ -77,7 +77,8 @@ export function MyCallsChart() {
       return recs || [];
     }).then(recs => {
       const counts = new Map<string, number>();
-      (recs || []).forEach((r: any) => {
+      (recs || []).forEach(r => {
+        if (!r.created_at) return;
         const key = r.created_at.slice(0, 10);
         counts.set(key, (counts.get(key) || 0) + 1);
       });
@@ -119,7 +120,7 @@ export function AttendanceTrendChart() {
       return recs || [];
     }).then(recs => {
       const counts = new Map<string, number>();
-      (recs || []).forEach((r: any) => counts.set(r.attendance_date, (counts.get(r.attendance_date) || 0) + 1));
+      (recs || []).forEach(r => counts.set(r.attendance_date, (counts.get(r.attendance_date) || 0) + 1));
       const days: { day: string; present: number }[] = [];
       for (let i = 13; i >= 0; i--) {
         const d = new Date(); d.setDate(d.getDate() - i);
@@ -158,12 +159,12 @@ export function LeadsFunnelChart({ segments }: { segments: Segment[] }) {
     }).then(leads => {
       if (!leads) return;
       const rows = segments.map(seg => {
-        const mine = leads.filter((l: any) => l.segment_slug === seg.slug);
+        const mine = leads.filter(l => l.segment_slug === seg.slug);
         return {
           segment: seg.name,
-          new: mine.filter((l: any) => l.stage === 'new').length,
-          contacted: mine.filter((l: any) => ['contacted', 'qualified', 'quoted'].includes(l.stage)).length,
-          won: mine.filter((l: any) => l.stage === 'won').length,
+          new: mine.filter(l => l.stage === 'new').length,
+          contacted: mine.filter(l => ['contacted', 'qualified', 'quoted'].includes(l.stage)).length,
+          won: mine.filter(l => l.stage === 'won').length,
           color: seg.color,
         };
       });
@@ -203,7 +204,7 @@ export function TicketStatusChart() {
     }).then(tickets => {
       if (!tickets) return;
       const counts = new Map<string, number>();
-      tickets.forEach((t: any) => counts.set(t.status, (counts.get(t.status) || 0) + 1));
+      tickets.forEach(t => counts.set(t.status, (counts.get(t.status) || 0) + 1));
       setData(Array.from(counts.entries()).map(([name, value]) => ({ name: name.replace('_', ' '), value })));
     }).catch(() => {});
   }, []);
