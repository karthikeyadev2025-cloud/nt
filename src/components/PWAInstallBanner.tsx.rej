diff a/src/components/PWAInstallBanner.tsx b/src/components/PWAInstallBanner.tsx	(rejected hunks)
@@ -17,7 +17,8 @@ export default function PWAInstallBanner() {
     if (alreadyDismissed) return;
 
     const isStandalone = window.matchMedia('(display-mode: standalone)').matches
-      || (navigator as any).standalone === true;
+      // Safari on iOS exposes a non-standard `standalone` bool on navigator.
+      || (navigator as Navigator & { standalone?: boolean }).standalone === true;
     if (isStandalone) return;
 
     const handler = (e: Event) => {
